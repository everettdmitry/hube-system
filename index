<!DOCTYPE html>
<html>
<head>
    <title>LAB ARCHIVE // AUTHENTICATION</title>
    <style>
        /* === OLD TERMINAL STYLE === */
        body {
            background-color: #0a0a0a;
            color: #00ff41;
            font-family: 'Courier New', monospace;
            font-size: 18px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            padding: 20px;
            box-sizing: border-box;
        }

        .login-box {
            border: 2px solid #00ff41;
            padding: 40px 50px;
            max-width: 520px;
            width: 100%;
            box-shadow: 0 0 30px rgba(0, 255, 65, 0.15);
            background-color: #0a0a0a;
        }

        .login-box h1 {
            text-align: center;
            font-size: 20px;
            font-weight: normal;
            letter-spacing: 3px;
            text-transform: uppercase;
            border-bottom: 1px solid #00ff41;
            padding-bottom: 12px;
            margin-top: 0;
        }

        .input-group {
            margin: 18px 0;
        }

        .input-group label {
            display: block;
            font-size: 13px;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: #00ff41;
            margin-bottom: 4px;
        }

        .input-group input {
            width: 100%;
            background: none;
            border: 1px solid #00ff41;
            color: #00ff41;
            font-family: 'Courier New', monospace;
            font-size: 16px;
            padding: 10px 12px;
            box-sizing: border-box;
            outline: none;
            transition: 0.3s;
        }

        .input-group input:focus {
            box-shadow: 0 0 15px rgba(0, 255, 65, 0.3);
        }

        .login-btn {
            width: 100%;
            background: none;
            border: 1px solid #00ff41;
            color: #00ff41;
            font-family: 'Courier New', monospace;
            font-size: 16px;
            padding: 12px;
            letter-spacing: 4px;
            text-transform: uppercase;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 10px;
        }

        .login-btn:hover {
            background-color: #00ff41;
            color: #0a0a0a;
            box-shadow: 0 0 30px rgba(0, 255, 65, 0.4);
        }

        .system-msg {
            text-align: center;
            font-size: 13px;
            color: #00aa33;
            margin-top: 18px;
            opacity: 0.7;
        }

        .cursor-blink {
            animation: blink 1.2s step-end infinite;
        }

        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }

        .warning-text {
            color: #ff2244;
            font-size: 13px;
            text-align: center;
            margin-top: 15px;
            display: none;
        }

        .ascii-art {
            text-align: center;
            font-size: 10px;
            line-height: 1.2;
            color: #00ff41;
            opacity: 0.5;
            margin-bottom: 8px;
            white-space: pre;
        }

        .hidden-hint {
            display: none;
        }

        /* Footer note */
        .footer-note {
            text-align: center;
            font-size: 11px;
            color: #005522;
            margin-top: 20px;
            opacity: 0.4;
            letter-spacing: 1px;
        }
    </style>
</head>
<body>

    <div class="login-box">

        <!-- ======================================== -->
        <!-- ASCII ART (looks like old computer logo) -->
        <!-- ======================================== -->

        <div class="ascii-art">
    ╔═══════════════════════════════════════╗
    ║   ██╗  ██╗██╗   ██╗██████╗ ███████╗ ║
    ║   ██║  ██║██║   ██║██╔══██╗██╔════╝ ║
    ║   ███████║██║   ██║██████╔╝█████╗   ║
    ║   ██╔══██║██║   ██║██╔══██╗██╔══╝   ║
    ║   ██║  ██║╚██████╔╝██████╔╝███████╗ ║
    ║   ╚═╝  ╚═╝ ╚═════╝ ╚═════╝ ╚══════╝ ║
    ╚═══════════════════════════════════════╝
        </div>

        <!-- ======================================== -->
        <!-- HEADER                                   -->
        <!-- ======================================== -->

        <h1>▌ LAB ARCHIVE // v.2.1.88 ▌</h1>

        <!-- ======================================== -->
        <!-- SYSTEM MESSAGE                           -->
        <!-- ======================================== -->

        <div class="system-msg">
            ⚡ SYSTEM SECURED. AUTHORIZATION REQUIRED.<br>
            STAFF ONLY. UNAUTHORIZED ACCESS PROHIBITED.
        </div>

        <br>

        <!-- ======================================== -->
        <!-- LOGIN FORM (DOES NOT ACTUALLY LOG IN)    -->
        <!-- ======================================== -->

        <form id="loginForm" onsubmit="return false;">

            <div class="input-group">
                <label for="username">👤 USERNAME</label>
                <input type="text" id="username" placeholder="ENTER USERNAME..." autofocus>
            </div>

            <div class="input-group">
                <label for="password">🔒 PASSWORD</label>
                <input type="password" id="password" placeholder="ENTER PASSWORD...">
            </div>

            <button type="submit" class="login-btn" id="loginBtn">
                ⚡ ACCESS ⚡
            </button>

        </form>

        <!-- ======================================== -->
        <!-- ERROR MESSAGE (appears on click)         -->
        <!-- ======================================== -->

        <div class="warning-text" id="errorMsg">
            ❌ AUTHENTICATION FAILED.<br>
            INVALID USERNAME OR PASSWORD.<br>
            <span style="font-size: 11px; opacity: 0.6;">ACCESS DENIED. PLEASE TRY AGAIN LATER.</span>
        </div>

        <!-- ======================================== -->
        <!-- TERMINAL CURSOR LINE                     -->
        <!-- ======================================== -->

        <div class="system-msg" style="margin-top: 25px; font-size: 12px; opacity: 0.4;">
            <span class="cursor-blink">_</span> AWAITING INPUT...
        </div>

        <!-- ======================================== -->
        <!-- FOOTER (ARG reference)                   -->
        <!-- ======================================== -->

        <div class="footer-note">
            [ A BORROWED VOICE // ARCHIVE SPIN-OFF ]
        </div>

        <!-- ======================================== -->
        <!-- HIDDEN HINT (only visible in source)     -->
        <!-- ======================================== -->

        <div class="hidden-hint">
            <!-- 
                HINT FOR THOSE WHO VIEW PAGE SOURCE:
                CHECK PAGE 47. "A BORROWED VOICE".
                SOMETHING IS HIDDEN THERE.
            -->
        </div>

    </div>

    <!-- ============================================ -->
    <!-- SCRIPT (simulates login, never grants access) -->
    <!-- ============================================ -->

    <script>
        const form = document.getElementById('loginForm');
        const usernameInput = document.getElementById('username');
        const passwordInput = document.getElementById('password');
        const errorMsg = document.getElementById('errorMsg');
        const loginBtn = document.getElementById('loginBtn');

        form.addEventListener('submit', function(event) {
            event.preventDefault();

            // Show error message
            errorMsg.style.display = 'block';

            // Shake effect on the box
            const box = document.querySelector('.login-box');
            box.style.transition = 'transform 0.08s';
            box.style.transform = 'translateX(-5px)';
            setTimeout(() => { box.style.transform = 'translateX(5px)'; }, 100);
            setTimeout(() => { box.style.transform = 'translateX(-4px)'; }, 200);
            setTimeout(() => { box.style.transform = 'translateX(4px)'; }, 300);
            setTimeout(() => { box.style.transform = 'translateX(0)'; }, 400);

            // Clear fields
            usernameInput.value = '';
            passwordInput.value = '';

            // Refocus on username
            setTimeout(() => {
                usernameInput.focus();
            }, 500);

            // Temporarily change button style
            loginBtn.textContent = '⛔ DENIED ⛔';
            loginBtn.style.borderColor = '#ff2244';
            loginBtn.style.color = '#ff2244';

            setTimeout(() => {
                loginBtn.textContent = '⚡ ACCESS ⚡';
                loginBtn.style.borderColor = '#00ff41';
                loginBtn.style.color = '#00ff41';
            }, 1500);
        });

        // Enter key triggers submit
        usernameInput.addEventListener('keydown', function(e) {
            if (e.key === 'Enter') {
                form.dispatchEvent(new Event('submit'));
            }
        });

        passwordInput.addEventListener('keydown', function(e) {
            if (e.key === 'Enter') {
                form.dispatchEvent(new Event('submit'));
            }
        });

        // Auto-focus on page load
        window.addEventListener('load', function() {
            usernameInput.focus();
        });

        // Hidden Easter egg in console
        console.log('🔍 HINT: Page 47. "A Borrowed Voice".');
    </script>

</body>
</html>
